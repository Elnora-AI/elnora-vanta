---
updated: never (template)
source: template — run /vanta-sync
---

<!--
  TEMPLATE — this file ships with the plugin and contains only fake example rows.
  Run `/vanta-sync` to generate the real vanta-tests.md next to this template
  (or in $VANTA_REFERENCES_DIR if set). The generated file is your org's live
  compliance posture — it is gitignored and must never be committed.

  Real test IDs are long Vanta test slugs. Every row below is fake.
-->

# Failing Tests (NEEDS_ATTENTION)

| Test ID | Name | Category |
|---------|------|----------|
| example-test-check-fake | Example failing check (fake) | Example category |
| example-another-test-fake | Another example check (fake) | Example category |

**Total failing: 0** — template placeholder. Run `/vanta-sync` for real data, or query live with `elnora-vanta tests list --status NEEDS_ATTENTION`.
