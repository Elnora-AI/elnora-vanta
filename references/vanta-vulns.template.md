---
updated: never (template)
source: template — run /vanta-sync
---

<!--
  TEMPLATE — this file ships with the plugin and contains only fake example rows.
  Run `/vanta-sync` to generate the real vanta-vulns.md next to this template
  (or in $VANTA_REFERENCES_DIR if set). The generated file is your org's live
  security posture — it is gitignored and must never be committed.

  Real rows carry real CVE IDs. Every row below is fake (CVE-0000-00000).
-->

# Vulnerabilities

## Summary

| Severity | Count | Fixable | Overdue |
|----------|-------|---------|---------|
| HIGH | 0 | 0 | 0 |
| MEDIUM | 0 | 0 | 0 |
| LOW | 0 | 0 | 0 |
| **Total** | **0** | **0** | **0** |

## Details

| CVE | Severity | CVSS | Fixable | SLA Deadline | Status |
|-----|----------|------|---------|--------------|--------|
| CVE-0000-00000 | HIGH | 0.0 | Yes | 0000-00-00 | Example (fake) |
| CVE-0000-00001 | LOW | 0.0 | No | 0000-00-00 | Example (fake) |

**Priority action**: none — template placeholder. Run `/vanta-sync` for real data, or query live with `elnora-vanta vulns list --severity HIGH --overdue`.
