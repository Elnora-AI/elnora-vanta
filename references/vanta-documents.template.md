---
updated: never (template)
source: template — run /vanta-sync
---

<!--
  TEMPLATE — this file ships with the plugin and contains only fake example rows.
  Run `/vanta-sync` to generate the real vanta-documents.md next to this template
  (or in $VANTA_REFERENCES_DIR if set). The generated file is your org's live
  compliance posture — it is gitignored and must never be committed.

  Real document IDs are Vanta document slugs. Every row below is fake.
-->

# Compliance Documents (0 total)

## By Category

| Category | Count |
|----------|-------|
| Example category | 0 |
| Another example category | 0 |

## Document List

| ID | Category |
|----|----------|
| example-document-fake | Example category |
| example-another-document-fake | Another example category |

Template placeholder. Run `/vanta-sync` for real data, or query live with `elnora-vanta documents list`.
